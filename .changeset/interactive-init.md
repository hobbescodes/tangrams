---
"tangrams": minor
---

Make `tangrams init` interactive by default, prompting for configuration values including validation library, source type, schema location, and what to generate. Use `--skip` flag to generate a template config file for manual editing.
